---
platform: linux

caches:
  - path: $HOME/.gradle/
  - path: $HOME/.m2/repository
  - path: $HOME/.android/sdk

params:
  TERM: dumb

inputs:
  - name: pr
outputs:
  - name: reports
  - name: generatedAPK
image_resource:
  type: docker-image
  source:
    repository: darkgeat/android-studio-gitlab-ci
    tag: '3.0.0'
run:
  path: sh
  user: root
  args:
    - -c
    - |
      cd $HOME/.android/cache
      ls -la